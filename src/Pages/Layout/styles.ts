import styled from "styled-components";

export const Root = styled.div`
    background: purple;
    display: grid;
    grid-template-columns: 1fr 3fr;
    min-width: 80vw;
    min-height: 80vh;
    border-radius: 8px;
`